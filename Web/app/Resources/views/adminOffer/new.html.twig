{% extends 'base.html.twig' %}

{% block body %}
    <div class="home_title">
        <p>Offer creation</p>
    </div>


    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="edit_poi" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_offer_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
