{% extends 'base.html.twig' %}


{% block body %}

    <div class="home_title">
        <p><a href="{{ path('admin_poi_edit', { 'id': poi.id }) }}">Edit {{ poi.name }}</p>
    </div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="edit_poi" type="submit" value="Save changes" />
    {{ form_end(edit_form) }}

    {#<ul class="edit_poi">#}
        {#<li>#}
            {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form) }}#}
        {#</li>#}
    {#</ul>#}
{% endblock %}
